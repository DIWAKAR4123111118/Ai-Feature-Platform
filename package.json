{
  "name": "ai-feature-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "pg": "^8.20.0",
    "prisma": "^5.22.0"
  }
}
