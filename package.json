{
  "name": "ai-feature-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@prisma/client": "^5.19.0",
    "prisma": "^5.19.0"
  }
}
